import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    root: {
        flex: 1,
    },
    barsRow: {
        marginVertical: 8,
        flexDirection: "row",
        alignItems: "flex-end",
    },
    labelsRow: {
        flexDirection: "row",
        alignItems: "flex-start",
    },
    centerText: {
        textAlign: "center",
    },
});
