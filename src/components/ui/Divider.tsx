import { View } from "react-native";

export const Divider = () => (
    <View
        style={{
            height: 10,
            width: 2,
            borderRadius: 4,
            backgroundColor: "#3f3f3f",
        }}
    />
);
