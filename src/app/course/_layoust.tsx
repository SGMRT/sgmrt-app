import { Slot } from "expo-router";

/**
 * Serves as a layout component for the course section, rendering nested routes using the Slot component.
 */
export default function CourseLayout() {
    return <Slot />;
}
