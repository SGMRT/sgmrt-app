import UpdateLoadingOverlay from "../updates/UpdateLoadingOverlay";

export default function Test() {
    // useEffect(() => {
    //     SplashScreen.hideAsync().catch(() => {});
    // }, []);

    return <UpdateLoadingOverlay visible={true} />;
}
